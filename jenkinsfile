pipeline {
    agent any

    environment {
        // Define Docker Hub credentials
        DOCKER_HUB_CREDENTIALS = 'dockerhub-repo'
        // Define Docker image name
        DOCKER_IMAGE_NAME = 'odaudu/ms1-app' // Update 'your-image-name' with your desired image name
        // Define the Bitbucket repository URL
        GITHUB_REPO_URL = 'https://github.com/ugbasawo/nvit-game.git'
    }

    stages {
        stage('Git Checkout') {
            steps {
                script {
                    echo 'Checking out code from githbu repository'
                }
                // Clone the github repository and specify the develop branch
                git credentialsId: 'github-credentials', url: GITHUB_REPO_URL, branch: 'main'
            }
        }

        stage('Build Docker I
